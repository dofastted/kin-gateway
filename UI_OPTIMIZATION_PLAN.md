# 种核 Console — UI 优化计划

> 基线：`console.html` 单页 + `/api/panel/*`  
> 原则：**干净 · 少文案 · 业务导航 · 组件可复用**  
> 不推倒重写；在现网单页上按阶段硬化，再视需要迁 React/shadcn。

---

## 0. 现状快照（2026-08-16）

| 项 | 状态 |
|----|------|
| 登录 / Cookie / 会话持久化 | ✅ 已修（单次 bootstrap + HttpOnly Cookie + disk session） |
| 可视化状态卡（spark / 运行态） | ✅ 已接入 |
| 侧栏 L0–L6 标签 | ⚠️ 开发编号外露，需改为业务名 |
| 总览 | ⚠️ 信息堆叠，缺告警条与快捷入口 |
| VM 详情 | ⚠️ 可用但不分 Tab |
| 用量 | ⚠️ 表格可用，缺近限额高亮统一样式 |
| 代理池 | ⚠️ 功能全，导入/探测交互偏操作台 |
| 路由策略 | ⚠️ 只读 JSON |
| 深链 / hash 路由 | ❌ 刷新丢页 |
| 双重登录 | ✅ 已消除 |

---

## 1. 设计规范（全程遵守）

### 1.1 视觉

- 暗色默认，背景分层：`bg` → `card` → `hover`
- 圆角：卡片 12–14px；按钮 8px；badge 999px
- 间距：页内 `16/20`，卡片网格 `12`
- 字号：标题 16–18 · 正文 13–14 · 辅助 11–12 · mono 11
- 状态色：运行 `emerald` · 警告 `amber` · 危险 `red` · 静止 `muted`

### 1.2 文案

- **少写说明**；标题即语义
- 禁止侧栏出现 `L0 · L1` 技术编号
- 空态一行；错误一行 + 可重试

### 1.3 交互

- 一次登录，刷新保持
- 卡片可点进详情；危险操作二次确认（后续）
- 全局：刷新 · 全量探测 放顶栏
- `fetch` 统一 `credentials: 'include'`

### 1.4 组件约定（单页内函数组件）

| 组件 | 职责 |
|------|------|
| `vmCard` / `vm-viz` | 集群可视化单元 |
| `summaryCards` | 数字摘要条 |
| `bar` / spark | 用量与活动 |
| `pageHeader` | 标题 + 次要操作（页内） |
| `empty` / `error` | 空态与错误 |

---

## 2. 信息架构（目标导航）

```
种核
├── 总览
├── 集群          ← 可视化状态
├── 虚拟机        ← 详情工作台
├── 用量
├── 代理池
├── 协议
└── 设置
```

侧栏只保留上述 7 项，去掉 layer-tag 编号。

---

## 3. 分阶段任务

### P0 — 鉴权与骨架（已完成）

- [x] 单次 `bootstrap`
- [x] Cookie + localStorage
- [x] 服务端 session 落盘
- [x] 登录 / 退出干净

### P1 — 导航与总览（本轮）

- [ ] 侧栏改为业务名，去 L* 标签
- [ ] hash 路由：`#/overview` `#/cluster` `#/vm/:id` …
- [ ] 总览：摘要卡 + 告警条 + 集群预览（最多 6 卡）+ 快捷探测
- [ ] 统一 `empty` / 加载占位

### P2 — 集群与详情

- [ ] 集群工具栏：搜索 / 运行筛选
- [ ] 详情 Tab：概览 | 账号 | 代理 | 操作
- [ ] 近限额 / 无 token / 代理失败 状态点规范

### P3 — 用量与代理池

- [ ] 用量表：near_limit 行高亮；95% 线说明收折
- [ ] 代理池：导入区折叠；表格为主
- [ ] 绑定态与 VM 名称反查显示

### P4 — 协议与设置

- [ ] 协议：Base URL / 鉴权方式一页展示（只读）
- [ ] 模型列表干净网格
- [ ] 设置：粘性 / 额度关键项表单化（可读可写若 API 支持）

### P5 — 打磨（可选）

- [ ] 骨架屏
- [ ] 操作 Toast 统一
- [ ] 迁 Vite + shadcn（仅当单页维护成本过高）

---

## 4. 本轮执行顺序（P1）

1. 改侧栏 IA + 去编号  
2. 加 hash 路由（刷新不丢页）  
3. 重做总览布局（摘要 / 告警 / 预览）  
4. 补空态组件  
5. 部署 `console.html` → 网关 static  

验收：

- 登录一次，刷新仍在原 hash 页  
- 侧栏无 L0/L1 字样  
- 总览可看到运行数、近限额告警、≤6 张预览卡  

---

## 5. 文件与部署

| 路径 | 说明 |
|------|------|
| `artifacts/kin-console-deploy/index.html` | 前端主文件 |
| `/opt/kin-gateway/gateway/public/console.html` | 网关托管 |
| 本计划 | `artifacts/kin-oauth/UI_OPTIMIZATION_PLAN.md` |

每完成一阶段，在本文件勾选，并简记日期。

---

## 6. 变更记录

| 日期 | 阶段 | 说明 |
|------|------|------|
| 2026-08-16 | P0 | 登录 Cookie / 会话持久化 / 去双 init |
| 2026-08-16 | 计划 | 本文件建立；进入 P1 |
