# 种核 Console — UI 优化计划

> 基线：`console.html` 单页 + `/api/panel/*`  
> 原则：**干净 · 少文案 · 业务导航 · 组件可复用**  
> 不推倒重写；在现网单页上按阶段硬化，再视需要迁 React/shadcn。

---

## 0. 现状快照（2026-08-17）

| 项 | 状态 |
|----|------|
| 登录 / Cookie / 会话持久化 | ✅ 已修（单次 bootstrap + HttpOnly Cookie + disk session） |
| 可视化状态卡（spark / 运行态） | ✅ 已接入 |
| 侧栏 L0–L6 标签 | ✅ 已改为业务名（总览/集群/虚拟机/导入/用量/代理池/协议/设置） |
| 总览 | ✅ 健康分 + 告警条 + 摘要卡 + ≤6 预览卡 |
| VM 详情 | ✅ Tab：概览 / 账号 / 代理 / 操作 |
| 用量 | ✅ near_limit 高亮 + CLI 探测文案 |
| 代理池 | ✅ 导入折叠 + 测通 与 CLI 探测区分 |
| 协议 / 模型 | ✅ 不伪装说明 + CLI harvest 来源 |
| 深链 / hash 路由 | ✅ `#/overview` 等，刷新不丢页 |
| 双重登录 | ✅ 已消除 |

---

## 1. 设计规范（全程遵守）

### 1.1 视觉

- 暗色默认，背景分层：`bg` → `card` → `hover`
- 圆角：卡片 12–14px；按钮 8px；badge 999px
- 间距：页内 `16/20`，卡片网格 `12`
- 字号：标题 16–18 · 正文 13–14 · 辅助 11–12 · mono 11
- 状态色：运行 `emerald` · 警告 `amber` · 危险 `red` · 静止 `muted`

### 1.2 文案

- **少写说明**；标题即语义
- 禁止侧栏出现 `L0 · L1` 技术编号
- 空态一行；错误一行 + 可重试
- **核心对齐**：「只信 CLI · 全部走虚拟机 · 不伪装 · 只协议转换 + 转发」

### 1.3 交互

- 一次登录，刷新保持
- 卡片可点进详情；危险操作二次确认（后续）
- 全局：刷新 · CLI 探测 放顶栏
- `fetch` 统一 `credentials: 'include'`

### 1.4 组件约定（单页内函数组件）

| 组件 | 职责 |
|------|------|
| `vmCard` / `vm-viz` | 集群可视化单元 |
| `summaryCards` | 数字摘要条 |
| `bar` / spark | 用量与活动 |
| `pageHeader` | 标题 + 次要操作（页内） |
| `empty` / `error` | 空态与错误 |
| `alert-bar` | 总览近限额 / 无可用 / 错误提示 |

---

## 2. 信息架构（目标导航）

```
种核
├── 总览
├── 集群          ← 可视化状态
├── 虚拟机        ← 详情工作台
├── 导入          ← sessionKey → CLI 续期
├── 用量
├── 代理池
├── 协议
└── 设置
```

侧栏只保留上述项，去掉 layer-tag 编号。

---

## 3. 分阶段任务

### P0 — 鉴权与骨架（已完成）

- [x] 单次 `bootstrap`
- [x] Cookie + localStorage
- [x] 服务端 session 落盘
- [x] 登录 / 退出干净

### P1 — 导航与总览（已完成）

- [x] 侧栏改为业务名，去 L* 标签
- [x] hash 路由：`#/overview` `#/cluster` `#/vm/:id` …
- [x] 总览：摘要卡 + 告警条 + 集群预览（最多 6 卡）+ 快捷探测
- [x] 统一 `empty` / 关键文案（空态一行 + 动作指引）

### P2 — 集群与详情（已完成）

- [x] 集群工具栏：搜索 / 运行筛选
- [x] 详情 Tab：概览 | 账号 | 代理 | 操作
- [x] 近限额 / 无 token / 代理失败 状态点规范

### P3 — 用量与代理池（已完成）

- [x] 用量表：near_limit 行高亮；安全线说明
- [x] 代理池：导入区折叠；表格为主；「测通」与 CLI 探测区分
- [x] 绑定态与 VM 名称反查显示

### P4 — 协议与设置（已完成）

- [x] 协议：Base URL / 鉴权 / 上游一页展示（只读）+「不伪装」说明
- [x] 模型列表干净网格 · 来源显示 CLI harvest
- [x] 设置：粘性 / 额度关键项表单化

### P5 — 打磨（可选）

- [ ] 骨架屏
- [x] 操作 Toast 统一（CLI 探测 / 代理探测 区分）
- [ ] 迁 Vite + shadcn（仅当单页维护成本过高）

---

## 4. 验收（已达成）

- 登录一次，刷新仍在原 hash 页  
- 侧栏无 L0/L1 字样  
- 总览可看到运行数、近限额告警、≤6 张预览卡  
- 文案全面对齐「只信 CLI · 全部走虚拟机 · 不伪装」

---

## 5. 文件与部署

| 路径 | 说明 |
|------|------|
| `kin-console/index.html` | 前端主文件（Vercel） |
| `/opt/kin-gateway/gateway/public/console.html` | 网关托管 |
| 本计划 | `kin-gateway/UI_OPTIMIZATION_PLAN.md` |

每完成一阶段，在本文件勾选，并简记日期。

---

## 6. 变更记录

| 日期 | 阶段 | 说明 |
|------|------|------|
| 2026-08-16 | P0 | 登录 Cookie / 会话持久化 / 去双 init |
| 2026-08-16 | 计划 | 本文件建立；进入 P1 |
| 2026-08-17 | P1–P4 | 告警条、预览≤6、文案对齐 CLI/不伪装、代理测通、模型 harvest 来源、Toast 区分、品牌「官方 Claude Code 转发」 |
